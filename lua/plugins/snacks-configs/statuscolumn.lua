return {
	enabled = true, -- تفعيل الميزة

	-- 🔽 ترتيب العناصر على اليسار (من الأعلى للأسفل في الأولوية)
	-- "mark": العلامات اليدوية
	-- "sign": العلامات العامة (أخطاء، تحذيرات، Git)
	left = { "mark", "sign" },

	-- 🔼 ترتيب العناصر على اليمين (بالقرب من النص)
	-- "fold": أيقونات الطي
	-- "git": إشارات Git المخصصة
	right = { "fold", "git" },

	-- 📂 إعدادات الطي (Folds)
	folds = {
		open = true, -- إظهار أيقونة حتى للفواتح المفتوحة (اختياري)
		git_hl = true, -- تلوين أيقونة الطي حسب حالة Git (مفيد جداً)
	},

	-- 🐙 إعدادات Git
	git = {
		-- الأنماط التي يتم التعرف عليها كإشارات Git
		patterns = { "GitSign", "MiniDiffSign" },
	},

	-- ⚡ الأداء
	refresh = 50, -- تحديث العمود كل 50ms لتقليل استهلاك الموارد
}
